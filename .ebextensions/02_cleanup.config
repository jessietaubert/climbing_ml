commands:
  01_cleanup_tmp:
    command: rm -rf /tmp/*
    ignoreErrors: true
  02_cleanup_var_cache:
    command: yum clean all && rm -rf /var/cache/yum
    ignoreErrors: true

container_commands:
  01_pip_cleanup:
    command: "/var/app/venv/*/bin/pip cache purge"
    ignoreErrors: true